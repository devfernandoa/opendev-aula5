from setuptools import setup

setup(name='Fernando Alzueta',
      version='0.1',
      packages=['dev_aberto'],
      scripts=['scripts/hello.py'],
      install_requires=[
            'requests'
        ]
      )
